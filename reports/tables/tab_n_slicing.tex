
% N-slicing experiment: Sentinel-2, K=2, varying number of input years N,
% 5-fold geographic CV.  N=all uses all acquisitions from 2016 through the
% year preceding the change (variable per tile; typically 4--8 years).
% Pooled metrics sum confusion matrices across all folds before computing metrics.
% Macro mean/std are computed per-fold then averaged -- secondary diagnostic.

\begin{table}[ht]
  \centering
  \caption{%
    Effect of input time-series length $N$ on land-take segmentation performance
    (Sentinel-2, prediction horizon $K = 2$ years, 5-fold geographic cross-validation).
    $N$ denotes the number of years of Sentinel-2 imagery used as input;
    $N = \text{all}$ uses every available acquisition year for each tile.
    Pooled metrics aggregate all test pixels across folds so that each tile
    contributes equally~\citep{garnot2021pastis}.
    $\text{IoU}_{\text{macro}}$ is the mean $\pm$ standard deviation of the
    per-fold IoU scores and is reported as a diagnostic.
    Best pooled value in each column is \textbf{bold}.%
  }
  \label{tab:n_slicing}
  \begin{tabular}{cccccl}
    \toprule
    $N$ (years) & IoU & F1 & Precision & Recall & $\text{IoU}_{\text{macro}} \pm \sigma$ \\
    \midrule
    1         & 0.106           & 0.191           & 0.194           & 0.188           & $0.094 \pm 0.052$ \\
    2         & 0.356           & 0.525           & 0.637           & 0.447           & $0.366 \pm 0.074$ \\
    3         & \textbf{0.414} & \textbf{0.586} & \textbf{0.673} & \textbf{0.519} & $0.404 \pm 0.041$ \\
    4         & 0.361           & 0.531           & 0.586           & 0.486           & $0.383 \pm 0.058$ \\
    \midrule
    all       & 0.334           & 0.501           & 0.646           & 0.409           & $0.345 \pm 0.078$ \\
    \bottomrule
  \end{tabular}
\end{table}
